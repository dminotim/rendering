# Просмотрщик сцен

Приложение поверх [dmrender](../dmrender) — обёртки над Vulkan и Metal. Обёртка раньше лежала в
этом же репозитории (`render_pipeline/`, `render_vulkan/`, `render_metal/`) и вынесена в отдельный;
здесь остался только рендерер.

```
main.cpp, FastRenderer.cpp   цикл кадра, камера, тени, интерфейс
mesh/                        загрузка геометрии: OBJ, STL, PLY, FBX, .dmscene + бинарный кеш
texture/                     загрузка и разделение текстур
shaders/glsl, shaders/metal  шейдеры этого приложения
tools/unity_import/          конвертация сцен из .unitypackage
```

## Сборка

Обёртку нужно склонировать рядом:

```sh
git clone --recurse-submodules <url-dmrender> ../dmrender
cmake -S . -B build
cmake --build build --config Release
```

CMake сам подхватит `../dmrender`. Если она лежит в другом месте — `-DDMRENDER_DIR=/path/to/dmrender`;
если установлена — `find_package(dmrender)` найдёт её и локальная копия не понадобится.

Требуется Vulkan SDK на Windows (в том числе `glslc`) и Xcode на macOS.

## Запуск

```sh
DMRENDER_MODEL=path/to/scene.obj  build/Release/RenderingApp
```

Без переменной окружения ищутся `assets/San_Miguel/san-miguel*.obj` и `bunny.obj`, поднимаясь на
несколько каталогов вверх — так работает и запуск из `build/Release`, и из корня проекта.

| Переменная | Что делает |
|---|---|
| `DMRENDER_MODEL` | Путь к сцене (`.obj`, `.stl`, `.ply`, `.fbx`, `.dmscene`) |
| `DMRENDER_VIEW` | `x,y,z[,yaw,pitch]` в метрах — стартовая точка камеры и ракурс для съёмки |
| `DMRENDER_SCREENSHOT` | Префикс пути: отрисовать в PNG и выйти, **без окна** |
| `DMRENDER_BENCH` | Число повторов на ракурс для замера времени кадра |
| `DMRENDER_FRAMES` | Закрыть окно после N кадров |
| `DMRENDER_NOSHADOW` | Запустить с выключенными тенями |
| `DMRENDER_CASTER_CULL` | Порог отбрасывания мелких загораживателей теней, в текселях |
| `DMRENDER_DUMP_CASCADES` | Выгрузить сами карты теней в PNG (диагностика) |

Переменные окружения живут дольше команды, которая их задала: `DMRENDER_SCREENSHOT` или
`DMRENDER_FRAMES`, забытые в оболочке, заставят следующий запуск закрыться сразу. Приложение
сообщает об этом при старте.

Управление: перетаскивание мышью — обзор, `WASD` — движение, `Q`/`E` — вниз/вверх, `Shift` —
быстро, `Ctrl` — медленно. Остальное в панели.

## Документация

| Документ | Что внутри |
|---|---|
| [docs/scenes/](docs/scenes/README.ru.md) | Книга: как пользоваться обёрткой — от треугольника до сцен архива casual-effects. Шейдеры на Metal |
| [tools/unity_import/](tools/unity_import/README.md) | Как достать раскладку сцены из `.unitypackage` |

Документация самой обёртки — справочник по API, книга о её внутреннем устройстве и обзор пайплайна
— переехала вместе с ней в [dmrender/docs](../dmrender/docs).

## Заметки

**Кеш сцены.** Первый разбор `san-miguel.obj` (1.1 ГБ текста) занимает около девяти секунд;
результат сохраняется рядом как `.dmcache`, и последующие запуски читают его за 0.25 с. Кеш
инвалидируется по размеру и времени изменения исходного файла.

**Ассеты в репозиторий не входят.** Скачайте архив со страницы сцены и распакуйте так, чтобы
получилось `assets/San_Miguel/san-miguel.obj` рядом с каталогом проекта.

**Тени.** Четыре каскада 2048×2048 для солнца. Хранится не глубина, а **расстояние в мировых
единицах** в цветовой цели `R32_FLOAT`: буфер глубины в этой обёртке нельзя сэмплировать, а
сравнивающего сэмплера нет. Зато смещения задаются в текселях и сантиметрах, а не в безразмерных
единицах буфера. Объём каскада подгоняется по ограничивающей **сфере** среза пирамиды видимости и
привязывается к сетке текселей — без этого края теней кипят при движении камеры. Смещение —
вдоль нормали, ручной PCF 3×3, отдельный вариант шейдера с `discard` для листвы.
